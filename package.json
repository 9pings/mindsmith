{
  "name": "skynet-dequantizer",
  "version": "0.2.0",
  "license": "AGPL-3.0-or-later",
  "author": "Nathanael Braun <pp9ping@gmail.com>",
  "description": "Your low-quant local model, repaired and steered like a bigger one — an OpenAI-compatible local proxy over the skynet-graph engine + your own room of verified .sgc stocks. Nothing leaves the machine (no-egress proven on real sockets).",
  "main": "./lib/app.js",
  "bin": {
    "skynet-dequantizer": "bin/skynet-dequantizer"
  },
  "scripts": {
    "start": "node bin/skynet-dequantizer serve",
    "test": "node --test --test-force-exit test/*.test.js"
  },
  "dependencies": {
    "skynet-graph": "file:../skynet-graph"
  }
}
